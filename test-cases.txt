-features
    -create table
    -remove key from column
    -add key to column
    -instead of "die()" function, create error flags => set error flag to true if error => if error flag true => echo backAnchor anchor element